services:
  portal-niq-dev:
    build: .
    container_name: portal-niq-dev
    environment:
      - VIRTUAL_HOST=portal.niq-dev.com
      - VIRTUAL_PORT=8080
      - LETSENCRYPT_HOST=portal.niq-dev.com
      - LETSENCRYPT_EMAIL=sakanasan20@gmail.com
    networks:
      - nginx-proxy
    restart: always

networks:
  nginx-proxy:
    external: true